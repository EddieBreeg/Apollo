AddLibrary(${PROJECT_NAME}Entry TYPE STATIC SOURCES GameEntry.cpp Entry.hpp
LINK PRIVATE ${PROJECT_NAME}Runtime
PROPERTIES ${COMMON_PROPERTIES}
DEFINITIONS PUBLIC APOLLO_GAME_ENTRY
OPTIONS PRIVATE ${COMPILER_ARGS}
)

AddLibrary(${PROJECT_NAME}EditorEntry TYPE STATIC SOURCES EditorEntry.cpp Entry.hpp
LINK PUBLIC "${PROJECT_NAME}Runtime" "${PROJECT_NAME}EditorRuntime"
PROPERTIES ${COMMON_PROPERTIES}
DEFINITIONS PUBLIC APOLLO_EDITOR_ENTRY
OPTIONS PRIVATE ${COMPILER_ARGS}
)