file(GLOB EDITOR_HEADERS *.h *.hpp)

AddLibrary(${PROJECT_NAME}EditorRuntime
SOURCES
	Editor.cpp
	${EDITOR_HEADERS}
DEFINITIONS PUBLIC BRK_EDITOR=1
PROPERTIES ${COMMON_PROPERTIES}
COMPILER_OPTIONS PRIVATE ${COMPILER_ARGS}
LINK PRIVATE ${PROJECT_NAME}Runtime
)